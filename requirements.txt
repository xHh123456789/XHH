# Backend runtime
fastapi==0.111.0
uvicorn==0.30.1
starlette==0.37.2
pydantic==2.13.4
sqlalchemy==2.0.31
pymysql==1.1.0
python-dotenv==1.0.0

# Auth: NOTE - bcrypt>=4.1 is incompatible with passlib 1.7.x (ValueError: password cannot be longer than 72 bytes)
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.5.0

# Deploy / migration tools
gunicorn==26.0.0
alembic==1.18.5
